# Workflow Configuration

# General workflow settings
workflow:
  name: "Primitive State Segmentation and Clustering"
  description: "Standard ML workflow for processing and training on input data."
  appliance_name: "dishwasher" 
  resume: true #有done会跳过已执行的step
  sequence_id: "20260513_220235"

# Step-specific configurations
steps:
  data_processor:
    enabled: false
    normalization: true
    save_figures: true

  wavelet_separation:
    enabled: true
    is_shape_dtw: false
    plot_count: 0

  feature_extract:
    enabled: true
    data_path: ""
    seq_len_path: ""
    model_name: "bilstm_ae"
    latent_dim: 64
    epochs: 50
    batch_size: 32
    learning_rate: 0.001
    patience: 5
    attention_size: 32
  
  time_clustering:
    enabled: true
    data_path: ""
    feature_path: ""
    seq_len_path: ""
    eps: 1.0
    min_pts: 20
    metric: "euclidean"
    normalization_method: "zscore"
    col_index: 2
  

# Model hyperparameters
models:
  DummyModel:
    learning_rate: 0.01
    epochs: 10
    batch_size: 32
